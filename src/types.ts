export interface initialValues {
    cardNumber: string
    name: string
    date: string
    cvv: string
}

export interface BlurValues {
    cardNumber: boolean
    name: boolean
    date: boolean
    cvv: boolean
}