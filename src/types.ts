export interface Values {
    cardNumber: number | null
    name: string | null
    date: number | null
    cvv: number | null
}

export interface BlurValues {
    cardNumber: boolean
    name: boolean
    date: boolean
    cvv: boolean
}